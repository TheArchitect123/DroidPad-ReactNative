import * as React from "react";
import { ScrollView, Text } from "react-native";

export const DashboardTabbedHomeComponent = ({ navigation }) => {
  return (
    <ScrollView>
      <Text>Home Page</Text>
    </ScrollView>
  );
};
