import * as React from "react";
import { ScrollView, Text } from "react-native";

export const DashboardTabbedSettingsComponent = ({ navigation }) => {
  return (
    <ScrollView>
      <Text>Settings Page</Text>
    </ScrollView>
  );
};
