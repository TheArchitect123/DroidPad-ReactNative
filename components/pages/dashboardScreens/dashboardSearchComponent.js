import * as React from "react";
import { ScrollView, Text } from "react-native";

export const DashboardTabbedSearchComponent = ({ navigation }) => {
  return (
    <ScrollView>
      <Text>Search Page</Text>
    </ScrollView>
  );
};
